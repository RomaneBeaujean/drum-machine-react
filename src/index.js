import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <h1>Drum Machine</h1>
  </React.StrictMode>,
  document.getElementById('root')
);
